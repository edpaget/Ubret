// libubret
// libubret 0.2.0
// libubret is distributed under the APL. See COPYING

(function () {

  var root = this;
  var previousUbret = root.Ubret;

  var Ubret = {};

  if (typeof exports !== 'undefined')
    module.exports = Ubret;
  else
    root.Ubret = Ubret;

  Ubret.Events = {}
}).call(this)
